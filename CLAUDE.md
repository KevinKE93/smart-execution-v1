# Smart Execution V1 For Claude

Claude Code can use this package through `.claude/commands/smart-execution-v1.md`.

Keep `SKILL.md` as the canonical source. The Claude command should point back to it instead of duplicating the full workflow.
